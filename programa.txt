inicio
    int i; float f; string s;

    F = (5 - 4.0) * (1 + 6) / (3); -- calma calabreso
    read(i); 
    soma(i, f); 
    print(s); 
    while (i) { 
        i = i - 1;
        if (i)  
            print("String, ó");
    }
fim
