inicio
    int i; float f; string s;

    F = (5 - 4.0) * (1 + 6) / (3); -- calma calabreso
    read(i); 
    j = (i, f); 
    print(s); 
    while (i) { --uau
        i = i - 1;
        if (i)  
            print("String, ó");
    }
fim
