inicio
    int i; float f; string s;

    f = (a - b) * (a + b) / (a);
    read(i);
    soma(i, f);
    print(s); 
    while (i) { 
        i = i - a;
        if (i) 
            print(String, ó);
    }
fim